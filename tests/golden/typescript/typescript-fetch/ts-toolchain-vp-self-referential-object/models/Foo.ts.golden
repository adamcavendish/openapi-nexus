/**
 * @generated by openapi-nexus. Do not edit.
 *
 * Self-Referential Object Test — 1.0.0
 */
import type { Bar, Bar$Wire } from './Bar';
import { barFromJSON, barToJSON } from './Bar';

export interface Foo$Wire {
  readonly children?: readonly Foo$Wire[];
  readonly related_bar: Bar$Wire;
}

export interface Foo {
  readonly children?: readonly Foo[];
  readonly relatedBar: Bar;
}

export function fooFromJSON(json: Foo$Wire): Foo {
  return {
    children: json.children !== undefined ? json.children.map((item) => fooFromJSON(item)) : undefined,
    relatedBar: barFromJSON(json.related_bar),
  };
}

export function fooToJSON(value: Foo): Foo$Wire {
  return {
    children: value.children !== undefined ? value.children.map((item) => fooToJSON(item)) : undefined,
    related_bar: barToJSON(value.relatedBar),
  };
}
