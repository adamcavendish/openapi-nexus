// @generated
// Code generated by openapi-nexus. DO NOT EDIT.
//
// Petstore API — 1.0.0
// This is a sample Pet Store Server based on the OpenAPI 3.1 specification

use crate::runtime::client::Client;
use crate::runtime::error::{ApiCallError, ApiError, Error};

/// API operations under the "pet" tag.
pub struct PetApi<'a, R: aioduct::RuntimePoll, C: aioduct::ConnectorSend> {
    client: &'a Client<R, C>,
}

impl<'a, R: aioduct::RuntimePoll, C: aioduct::ConnectorSend> PetApi<'a, R, C> {
    /// Create a new `PetApi` bound to the given client.
    pub fn new(client: &'a Client<R, C>) -> Self {
        Self {
            client,
        }
    }

    /// Update an existing pet
    pub async fn update_pet(
        &self,
        body: &crate::models::Pet,
    ) -> Result<UpdatePetResponse, UpdatePetError> {
        let path = "/pet".to_string();
        let mut req = self.client.put(&path)?;
        req = req.json(&body).map_err(Error::from)?;
        let resp = req.send().await.map_err(Error::from)?;
        let status_code = resp.status().as_u16();
        let response_headers = resp.headers().clone();
        let body_bytes = resp.bytes().await.map_err(Error::from)?;
        let mut result = UpdatePetResponse { status_code, headers: response_headers.clone(), data: None };
        match status_code {
            200 => {
                result.data = Some(serde_json::from_slice(&body_bytes).map_err(Error::Deserialize)?);
            }
            _ => {}
        }
        if !(200..300).contains(&status_code) {
            match status_code {
                400 => {
                    let body = Ok::<(), Error>(());
                    return Err(UpdatePetError::BadRequest(ApiError::new(status_code, response_headers.clone(), body_bytes.to_vec(), body)));
                }
                404 => {
                    let body = Ok::<(), Error>(());
                    return Err(UpdatePetError::NotFound(ApiError::new(status_code, response_headers.clone(), body_bytes.to_vec(), body)));
                }
                422 => {
                    let body = Ok::<(), Error>(());
                    return Err(UpdatePetError::Validation(ApiError::new(status_code, response_headers.clone(), body_bytes.to_vec(), body)));
                }
                _ => {
                    let body = Ok::<Vec<u8>, Error>(body_bytes.to_vec());
                    return Err(UpdatePetError::Unexpected(ApiError::new(status_code, response_headers.clone(), body_bytes.to_vec(), body)));
                }
            }
        }
        Ok(result)
    }

    /// Add a new pet to the store
    pub async fn add_pet(
        &self,
        body: &crate::models::Pet,
    ) -> Result<AddPetResponse, AddPetError> {
        let path = "/pet".to_string();
        let mut req = self.client.post(&path)?;
        req = req.json(&body).map_err(Error::from)?;
        let resp = req.send().await.map_err(Error::from)?;
        let status_code = resp.status().as_u16();
        let response_headers = resp.headers().clone();
        let body_bytes = resp.bytes().await.map_err(Error::from)?;
        let mut result = AddPetResponse { status_code, headers: response_headers.clone(), data: None };
        match status_code {
            200 => {
                result.data = Some(serde_json::from_slice(&body_bytes).map_err(Error::Deserialize)?);
            }
            _ => {}
        }
        if !(200..300).contains(&status_code) {
            match status_code {
                400 => {
                    let body = Ok::<(), Error>(());
                    return Err(AddPetError::BadRequest(ApiError::new(status_code, response_headers.clone(), body_bytes.to_vec(), body)));
                }
                422 => {
                    let body = Ok::<(), Error>(());
                    return Err(AddPetError::Validation(ApiError::new(status_code, response_headers.clone(), body_bytes.to_vec(), body)));
                }
                _ => {
                    let body = Ok::<Vec<u8>, Error>(body_bytes.to_vec());
                    return Err(AddPetError::Unexpected(ApiError::new(status_code, response_headers.clone(), body_bytes.to_vec(), body)));
                }
            }
        }
        Ok(result)
    }

    /// Find pets by status
    pub async fn find_pets_by_status(
        &self,
        status: &str,
    ) -> Result<FindPetsByStatusResponse, FindPetsByStatusError> {
        let mut path = "/pet/findByStatus".to_string();
        let mut query_parts: Vec<(&str, String)> = Vec::new();
        query_parts.push(("status", status.to_string()));
        if !query_parts.is_empty() {
            let qs = url::form_urlencoded::Serializer::new(String::new()).extend_pairs(query_parts.iter().map(|(k, v)| (*k, v.as_str()))).finish();
            path = format!("{}?{}", path, qs);
        }
        let req = self.client.get(&path)?;
        let resp = req.send().await.map_err(Error::from)?;
        let status_code = resp.status().as_u16();
        let response_headers = resp.headers().clone();
        let body_bytes = resp.bytes().await.map_err(Error::from)?;
        let mut result = FindPetsByStatusResponse { status_code, headers: response_headers.clone(), data: None };
        match status_code {
            200 => {
                result.data = Some(serde_json::from_slice(&body_bytes).map_err(Error::Deserialize)?);
            }
            _ => {}
        }
        if !(200..300).contains(&status_code) {
            match status_code {
                400 => {
                    let body = Ok::<(), Error>(());
                    return Err(FindPetsByStatusError::BadRequest(ApiError::new(status_code, response_headers.clone(), body_bytes.to_vec(), body)));
                }
                _ => {
                    let body = Ok::<Vec<u8>, Error>(body_bytes.to_vec());
                    return Err(FindPetsByStatusError::Unexpected(ApiError::new(status_code, response_headers.clone(), body_bytes.to_vec(), body)));
                }
            }
        }
        Ok(result)
    }

    /// Find pets by tags
    pub async fn find_pets_by_tags(
        &self,
        tags: &[String],
    ) -> Result<FindPetsByTagsResponse, FindPetsByTagsError> {
        let mut path = "/pet/findByTags".to_string();
        let mut query_parts: Vec<(&str, String)> = Vec::new();
        query_parts.push(("tags", tags.iter().map(ToString::to_string).collect::<Vec<_>>().join(",")));
        if !query_parts.is_empty() {
            let qs = url::form_urlencoded::Serializer::new(String::new()).extend_pairs(query_parts.iter().map(|(k, v)| (*k, v.as_str()))).finish();
            path = format!("{}?{}", path, qs);
        }
        let req = self.client.get(&path)?;
        let resp = req.send().await.map_err(Error::from)?;
        let status_code = resp.status().as_u16();
        let response_headers = resp.headers().clone();
        let body_bytes = resp.bytes().await.map_err(Error::from)?;
        let mut result = FindPetsByTagsResponse { status_code, headers: response_headers.clone(), data: None };
        match status_code {
            200 => {
                result.data = Some(serde_json::from_slice(&body_bytes).map_err(Error::Deserialize)?);
            }
            _ => {}
        }
        if !(200..300).contains(&status_code) {
            match status_code {
                400 => {
                    let body = Ok::<(), Error>(());
                    return Err(FindPetsByTagsError::BadRequest(ApiError::new(status_code, response_headers.clone(), body_bytes.to_vec(), body)));
                }
                _ => {
                    let body = Ok::<Vec<u8>, Error>(body_bytes.to_vec());
                    return Err(FindPetsByTagsError::Unexpected(ApiError::new(status_code, response_headers.clone(), body_bytes.to_vec(), body)));
                }
            }
        }
        Ok(result)
    }

    /// Find pet by ID
    pub async fn get_pet_by_id(
        &self,
        pet_id: i64,
    ) -> Result<GetPetByIdResponse, GetPetByIdError> {
        let mut path = "/pet/{petId}".to_string();
        path = path.replace("{petId}", &pet_id.to_string());
        let req = self.client.get(&path)?;
        let resp = req.send().await.map_err(Error::from)?;
        let status_code = resp.status().as_u16();
        let response_headers = resp.headers().clone();
        let body_bytes = resp.bytes().await.map_err(Error::from)?;
        let mut result = GetPetByIdResponse { status_code, headers: response_headers.clone(), data: None };
        match status_code {
            200 => {
                result.data = Some(serde_json::from_slice(&body_bytes).map_err(Error::Deserialize)?);
            }
            _ => {}
        }
        if !(200..300).contains(&status_code) {
            match status_code {
                400 => {
                    let body = Ok::<(), Error>(());
                    return Err(GetPetByIdError::BadRequest(ApiError::new(status_code, response_headers.clone(), body_bytes.to_vec(), body)));
                }
                404 => {
                    let body = Ok::<(), Error>(());
                    return Err(GetPetByIdError::NotFound(ApiError::new(status_code, response_headers.clone(), body_bytes.to_vec(), body)));
                }
                _ => {
                    let body = Ok::<Vec<u8>, Error>(body_bytes.to_vec());
                    return Err(GetPetByIdError::Unexpected(ApiError::new(status_code, response_headers.clone(), body_bytes.to_vec(), body)));
                }
            }
        }
        Ok(result)
    }

    /// Update a pet in the store with form data
    pub async fn update_pet_with_form(
        &self,
        pet_id: i64,
        name: &Option<String>,
        status: &Option<String>,
    ) -> Result<UpdatePetWithFormResponse, UpdatePetWithFormError> {
        let mut path = "/pet/{petId}".to_string();
        path = path.replace("{petId}", &pet_id.to_string());
        let mut query_parts: Vec<(&str, String)> = Vec::new();
        if let Some(v) = &name {
            query_parts.push(("name", v.to_string()));
        }
        if let Some(v) = &status {
            query_parts.push(("status", v.to_string()));
        }
        if !query_parts.is_empty() {
            let qs = url::form_urlencoded::Serializer::new(String::new()).extend_pairs(query_parts.iter().map(|(k, v)| (*k, v.as_str()))).finish();
            path = format!("{}?{}", path, qs);
        }
        let req = self.client.post(&path)?;
        let resp = req.send().await.map_err(Error::from)?;
        let status_code = resp.status().as_u16();
        let response_headers = resp.headers().clone();
        let body_bytes = resp.bytes().await.map_err(Error::from)?;
        let mut result = UpdatePetWithFormResponse { status_code, headers: response_headers.clone(), data: None };
        match status_code {
            200 => {
                result.data = Some(serde_json::from_slice(&body_bytes).map_err(Error::Deserialize)?);
            }
            _ => {}
        }
        if !(200..300).contains(&status_code) {
            match status_code {
                400 => {
                    let body = Ok::<(), Error>(());
                    return Err(UpdatePetWithFormError::BadRequest(ApiError::new(status_code, response_headers.clone(), body_bytes.to_vec(), body)));
                }
                _ => {
                    let body = Ok::<Vec<u8>, Error>(body_bytes.to_vec());
                    return Err(UpdatePetWithFormError::Unexpected(ApiError::new(status_code, response_headers.clone(), body_bytes.to_vec(), body)));
                }
            }
        }
        Ok(result)
    }

    /// Delete a pet
    pub async fn delete_pet(
        &self,
        pet_id: i64,
    ) -> Result<DeletePetResponse, DeletePetError> {
        let mut path = "/pet/{petId}".to_string();
        path = path.replace("{petId}", &pet_id.to_string());
        let req = self.client.delete(&path)?;
        let resp = req.send().await.map_err(Error::from)?;
        let status_code = resp.status().as_u16();
        let response_headers = resp.headers().clone();
        let result = DeletePetResponse { status_code, headers: response_headers.clone() };
        if !(200..300).contains(&status_code) {
            let body_bytes = resp.bytes().await.map_err(Error::from)?;
            if !(200..300).contains(&status_code) {
                match status_code {
                    400 => {
                        let body = Ok::<(), Error>(());
                        return Err(DeletePetError::BadRequest(ApiError::new(status_code, response_headers.clone(), body_bytes.to_vec(), body)));
                    }
                    _ => {
                        let body = Ok::<Vec<u8>, Error>(body_bytes.to_vec());
                        return Err(DeletePetError::Unexpected(ApiError::new(status_code, response_headers.clone(), body_bytes.to_vec(), body)));
                    }
                }
            }
        }
        Ok(result)
    }

    /// Upload an image
    pub async fn upload_file(
        &self,
        pet_id: i64,
        additional_metadata: &Option<String>,
    ) -> Result<UploadFileResponse, UploadFileError> {
        let mut path = "/pet/{petId}/uploadImage".to_string();
        path = path.replace("{petId}", &pet_id.to_string());
        let mut query_parts: Vec<(&str, String)> = Vec::new();
        if let Some(v) = &additional_metadata {
            query_parts.push(("additionalMetadata", v.to_string()));
        }
        if !query_parts.is_empty() {
            let qs = url::form_urlencoded::Serializer::new(String::new()).extend_pairs(query_parts.iter().map(|(k, v)| (*k, v.as_str()))).finish();
            path = format!("{}?{}", path, qs);
        }
        let req = self.client.post(&path)?;
        let resp = req.send().await.map_err(Error::from)?;
        let status_code = resp.status().as_u16();
        let response_headers = resp.headers().clone();
        let body_bytes = resp.bytes().await.map_err(Error::from)?;
        let mut result = UploadFileResponse { status_code, headers: response_headers.clone(), data: None };
        match status_code {
            200 => {
                result.data = Some(serde_json::from_slice(&body_bytes).map_err(Error::Deserialize)?);
            }
            _ => {}
        }
        if !(200..300).contains(&status_code) {
            match status_code {
                _ => {
                    let body = Ok::<Vec<u8>, Error>(body_bytes.to_vec());
                    return Err(UploadFileError::Unexpected(ApiError::new(status_code, response_headers.clone(), body_bytes.to_vec(), body)));
                }
            }
        }
        Ok(result)
    }
}

/// Response from `update_pet`.
#[derive(Debug)]
pub struct UpdatePetResponse {
    pub status_code: u16,
    pub headers: aioduct::HeaderMap,
    pub data: Option<crate::models::Pet>,
}

/// Error from `update_pet`.
#[derive(Debug)]
pub enum UpdatePetError {
    BadRequest(ApiError<()>),
    NotFound(ApiError<()>),
    Validation(ApiError<()>),
    Unexpected(ApiError<Vec<u8>>),
    Transport(Error),
}

impl From<Error> for UpdatePetError {
    fn from(error: Error) -> Self {
        Self::Transport(error)
    }
}

impl From<UpdatePetError> for ApiCallError {
    fn from(error: UpdatePetError) -> Self {
        match error {
            UpdatePetError::BadRequest(error) => Self::from_api_error("update_pet", error),
            UpdatePetError::NotFound(error) => Self::from_api_error("update_pet", error),
            UpdatePetError::Validation(error) => Self::from_api_error("update_pet", error),
            UpdatePetError::Unexpected(error) => Self::from_api_error("update_pet", error),
            UpdatePetError::Transport(error) => Self::from_runtime_error("update_pet", error),
        }
    }
}

impl std::fmt::Display for UpdatePetError {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        match self {
            Self::BadRequest(err) => write!(f, "HTTP error {}", err.status_code()),
            Self::NotFound(err) => write!(f, "HTTP error {}", err.status_code()),
            Self::Validation(err) => write!(f, "HTTP error {}", err.status_code()),
            Self::Unexpected(err) => write!(f, "unexpected HTTP error {}", err.status_code()),
            Self::Transport(err) => std::fmt::Display::fmt(err, f),
        }
    }
}

impl std::error::Error for UpdatePetError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match self {
            Self::Transport(err) => Some(err),
            _ => None,
        }
    }
}

/// Response from `add_pet`.
#[derive(Debug)]
pub struct AddPetResponse {
    pub status_code: u16,
    pub headers: aioduct::HeaderMap,
    pub data: Option<crate::models::Pet>,
}

/// Error from `add_pet`.
#[derive(Debug)]
pub enum AddPetError {
    BadRequest(ApiError<()>),
    Validation(ApiError<()>),
    Unexpected(ApiError<Vec<u8>>),
    Transport(Error),
}

impl From<Error> for AddPetError {
    fn from(error: Error) -> Self {
        Self::Transport(error)
    }
}

impl From<AddPetError> for ApiCallError {
    fn from(error: AddPetError) -> Self {
        match error {
            AddPetError::BadRequest(error) => Self::from_api_error("add_pet", error),
            AddPetError::Validation(error) => Self::from_api_error("add_pet", error),
            AddPetError::Unexpected(error) => Self::from_api_error("add_pet", error),
            AddPetError::Transport(error) => Self::from_runtime_error("add_pet", error),
        }
    }
}

impl std::fmt::Display for AddPetError {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        match self {
            Self::BadRequest(err) => write!(f, "HTTP error {}", err.status_code()),
            Self::Validation(err) => write!(f, "HTTP error {}", err.status_code()),
            Self::Unexpected(err) => write!(f, "unexpected HTTP error {}", err.status_code()),
            Self::Transport(err) => std::fmt::Display::fmt(err, f),
        }
    }
}

impl std::error::Error for AddPetError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match self {
            Self::Transport(err) => Some(err),
            _ => None,
        }
    }
}

/// Response from `find_pets_by_status`.
#[derive(Debug)]
pub struct FindPetsByStatusResponse {
    pub status_code: u16,
    pub headers: aioduct::HeaderMap,
    pub data: Option<Vec<crate::models::Pet>>,
}

/// Error from `find_pets_by_status`.
#[derive(Debug)]
pub enum FindPetsByStatusError {
    BadRequest(ApiError<()>),
    Unexpected(ApiError<Vec<u8>>),
    Transport(Error),
}

impl From<Error> for FindPetsByStatusError {
    fn from(error: Error) -> Self {
        Self::Transport(error)
    }
}

impl From<FindPetsByStatusError> for ApiCallError {
    fn from(error: FindPetsByStatusError) -> Self {
        match error {
            FindPetsByStatusError::BadRequest(error) => Self::from_api_error("find_pets_by_status", error),
            FindPetsByStatusError::Unexpected(error) => Self::from_api_error("find_pets_by_status", error),
            FindPetsByStatusError::Transport(error) => Self::from_runtime_error("find_pets_by_status", error),
        }
    }
}

impl std::fmt::Display for FindPetsByStatusError {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        match self {
            Self::BadRequest(err) => write!(f, "HTTP error {}", err.status_code()),
            Self::Unexpected(err) => write!(f, "unexpected HTTP error {}", err.status_code()),
            Self::Transport(err) => std::fmt::Display::fmt(err, f),
        }
    }
}

impl std::error::Error for FindPetsByStatusError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match self {
            Self::Transport(err) => Some(err),
            _ => None,
        }
    }
}

/// Response from `find_pets_by_tags`.
#[derive(Debug)]
pub struct FindPetsByTagsResponse {
    pub status_code: u16,
    pub headers: aioduct::HeaderMap,
    pub data: Option<Vec<crate::models::Pet>>,
}

/// Error from `find_pets_by_tags`.
#[derive(Debug)]
pub enum FindPetsByTagsError {
    BadRequest(ApiError<()>),
    Unexpected(ApiError<Vec<u8>>),
    Transport(Error),
}

impl From<Error> for FindPetsByTagsError {
    fn from(error: Error) -> Self {
        Self::Transport(error)
    }
}

impl From<FindPetsByTagsError> for ApiCallError {
    fn from(error: FindPetsByTagsError) -> Self {
        match error {
            FindPetsByTagsError::BadRequest(error) => Self::from_api_error("find_pets_by_tags", error),
            FindPetsByTagsError::Unexpected(error) => Self::from_api_error("find_pets_by_tags", error),
            FindPetsByTagsError::Transport(error) => Self::from_runtime_error("find_pets_by_tags", error),
        }
    }
}

impl std::fmt::Display for FindPetsByTagsError {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        match self {
            Self::BadRequest(err) => write!(f, "HTTP error {}", err.status_code()),
            Self::Unexpected(err) => write!(f, "unexpected HTTP error {}", err.status_code()),
            Self::Transport(err) => std::fmt::Display::fmt(err, f),
        }
    }
}

impl std::error::Error for FindPetsByTagsError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match self {
            Self::Transport(err) => Some(err),
            _ => None,
        }
    }
}

/// Response from `get_pet_by_id`.
#[derive(Debug)]
pub struct GetPetByIdResponse {
    pub status_code: u16,
    pub headers: aioduct::HeaderMap,
    pub data: Option<crate::models::Pet>,
}

/// Error from `get_pet_by_id`.
#[derive(Debug)]
pub enum GetPetByIdError {
    BadRequest(ApiError<()>),
    NotFound(ApiError<()>),
    Unexpected(ApiError<Vec<u8>>),
    Transport(Error),
}

impl From<Error> for GetPetByIdError {
    fn from(error: Error) -> Self {
        Self::Transport(error)
    }
}

impl From<GetPetByIdError> for ApiCallError {
    fn from(error: GetPetByIdError) -> Self {
        match error {
            GetPetByIdError::BadRequest(error) => Self::from_api_error("get_pet_by_id", error),
            GetPetByIdError::NotFound(error) => Self::from_api_error("get_pet_by_id", error),
            GetPetByIdError::Unexpected(error) => Self::from_api_error("get_pet_by_id", error),
            GetPetByIdError::Transport(error) => Self::from_runtime_error("get_pet_by_id", error),
        }
    }
}

impl std::fmt::Display for GetPetByIdError {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        match self {
            Self::BadRequest(err) => write!(f, "HTTP error {}", err.status_code()),
            Self::NotFound(err) => write!(f, "HTTP error {}", err.status_code()),
            Self::Unexpected(err) => write!(f, "unexpected HTTP error {}", err.status_code()),
            Self::Transport(err) => std::fmt::Display::fmt(err, f),
        }
    }
}

impl std::error::Error for GetPetByIdError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match self {
            Self::Transport(err) => Some(err),
            _ => None,
        }
    }
}

/// Response from `update_pet_with_form`.
#[derive(Debug)]
pub struct UpdatePetWithFormResponse {
    pub status_code: u16,
    pub headers: aioduct::HeaderMap,
    pub data: Option<crate::models::Pet>,
}

/// Error from `update_pet_with_form`.
#[derive(Debug)]
pub enum UpdatePetWithFormError {
    BadRequest(ApiError<()>),
    Unexpected(ApiError<Vec<u8>>),
    Transport(Error),
}

impl From<Error> for UpdatePetWithFormError {
    fn from(error: Error) -> Self {
        Self::Transport(error)
    }
}

impl From<UpdatePetWithFormError> for ApiCallError {
    fn from(error: UpdatePetWithFormError) -> Self {
        match error {
            UpdatePetWithFormError::BadRequest(error) => Self::from_api_error("update_pet_with_form", error),
            UpdatePetWithFormError::Unexpected(error) => Self::from_api_error("update_pet_with_form", error),
            UpdatePetWithFormError::Transport(error) => Self::from_runtime_error("update_pet_with_form", error),
        }
    }
}

impl std::fmt::Display for UpdatePetWithFormError {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        match self {
            Self::BadRequest(err) => write!(f, "HTTP error {}", err.status_code()),
            Self::Unexpected(err) => write!(f, "unexpected HTTP error {}", err.status_code()),
            Self::Transport(err) => std::fmt::Display::fmt(err, f),
        }
    }
}

impl std::error::Error for UpdatePetWithFormError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match self {
            Self::Transport(err) => Some(err),
            _ => None,
        }
    }
}

/// Response from `delete_pet`.
#[derive(Debug)]
pub struct DeletePetResponse {
    pub status_code: u16,
    pub headers: aioduct::HeaderMap,
}

/// Error from `delete_pet`.
#[derive(Debug)]
pub enum DeletePetError {
    BadRequest(ApiError<()>),
    Unexpected(ApiError<Vec<u8>>),
    Transport(Error),
}

impl From<Error> for DeletePetError {
    fn from(error: Error) -> Self {
        Self::Transport(error)
    }
}

impl From<DeletePetError> for ApiCallError {
    fn from(error: DeletePetError) -> Self {
        match error {
            DeletePetError::BadRequest(error) => Self::from_api_error("delete_pet", error),
            DeletePetError::Unexpected(error) => Self::from_api_error("delete_pet", error),
            DeletePetError::Transport(error) => Self::from_runtime_error("delete_pet", error),
        }
    }
}

impl std::fmt::Display for DeletePetError {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        match self {
            Self::BadRequest(err) => write!(f, "HTTP error {}", err.status_code()),
            Self::Unexpected(err) => write!(f, "unexpected HTTP error {}", err.status_code()),
            Self::Transport(err) => std::fmt::Display::fmt(err, f),
        }
    }
}

impl std::error::Error for DeletePetError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match self {
            Self::Transport(err) => Some(err),
            _ => None,
        }
    }
}

/// Response from `upload_file`.
#[derive(Debug)]
pub struct UploadFileResponse {
    pub status_code: u16,
    pub headers: aioduct::HeaderMap,
    pub data: Option<crate::models::UploadResponse>,
}

/// Error from `upload_file`.
#[derive(Debug)]
pub enum UploadFileError {
    Unexpected(ApiError<Vec<u8>>),
    Transport(Error),
}

impl From<Error> for UploadFileError {
    fn from(error: Error) -> Self {
        Self::Transport(error)
    }
}

impl From<UploadFileError> for ApiCallError {
    fn from(error: UploadFileError) -> Self {
        match error {
            UploadFileError::Unexpected(error) => Self::from_api_error("upload_file", error),
            UploadFileError::Transport(error) => Self::from_runtime_error("upload_file", error),
        }
    }
}

impl std::fmt::Display for UploadFileError {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        match self {
            Self::Unexpected(err) => write!(f, "unexpected HTTP error {}", err.status_code()),
            Self::Transport(err) => std::fmt::Display::fmt(err, f),
        }
    }
}

impl std::error::Error for UploadFileError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match self {
            Self::Transport(err) => Some(err),
            _ => None,
        }
    }
}
