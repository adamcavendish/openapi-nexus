// @generated
// Code generated by openapi-nexus. DO NOT EDIT.
//
// MultilineDocs — 1.0.0
// A test fixture for multi-line doc comments
// and primitive type alias suppression.

use crate::runtime::client::Client;
use crate::runtime::error::{ApiError, Error};

/// API operations under the "default" tag.
pub struct DefaultApi<'a, R: aioduct::RuntimePoll, C: aioduct::ConnectorSend> {
    client: &'a Client<R, C>,
}

impl<'a, R: aioduct::RuntimePoll, C: aioduct::ConnectorSend> DefaultApi<'a, R, C> {
    /// Create a new `DefaultApi` bound to the given client.
    pub fn new(client: &'a Client<R, C>) -> Self {
        Self {
            client,
        }
    }

    /// Accept a node into the pool by creating or replacing the resource.
    /// The controller converges the Node labels and taint from the CR.
    ///
    /// This operation is idempotent. Calling it multiple times
    /// with the same payload produces the same result.
    ///
    /// Empty lines above are preserved as paragraph breaks.
    pub async fn accept_node(
        &self,
        node_id: &str,
        body: &crate::models::AcceptNodeRequest,
    ) -> Result<AcceptNodeResponse, AcceptNodeError> {
        let mut path = "/nodes/{nodeId}/accept".to_string();
        path = path.replace("{nodeId}", &node_id.to_string());
        let mut req = self.client.post(&path)?;
        req = req.json(&body).map_err(Error::from)?;
        let resp = req.send().await.map_err(Error::from)?;
        let status_code = resp.status().as_u16();
        let response_headers = resp.headers().clone();
        let body_bytes = resp.bytes().await.map_err(Error::from)?;
        let mut result = AcceptNodeResponse { status_code, headers: response_headers.clone(), data: None };
        match status_code {
            200 => {
                result.data = Some(serde_json::from_slice(&body_bytes).map_err(Error::Deserialize)?);
            }
            _ => {}
        }
        if !(200..300).contains(&status_code) {
            match status_code {
                _ => {
                    let body = Ok::<Vec<u8>, Error>(body_bytes.to_vec());
                    return Err(AcceptNodeError::Unexpected(ApiError::new(status_code, response_headers.clone(), body_bytes.to_vec(), body)));
                }
            }
        }
        Ok(result)
    }

    /// Get the name of a node (returns a primitive string alias).
    pub async fn get_node_name(
        &self,
        node_id: &str,
    ) -> Result<GetNodeNameResponse, GetNodeNameError> {
        let mut path = "/nodes/{nodeId}/name".to_string();
        path = path.replace("{nodeId}", &node_id.to_string());
        let req = self.client.get(&path)?;
        let resp = req.send().await.map_err(Error::from)?;
        let status_code = resp.status().as_u16();
        let response_headers = resp.headers().clone();
        let body_bytes = resp.bytes().await.map_err(Error::from)?;
        let mut result = GetNodeNameResponse { status_code, headers: response_headers.clone(), data: None };
        match status_code {
            200 => {
                result.data = Some(serde_json::from_slice(&body_bytes).map_err(Error::Deserialize)?);
            }
            _ => {}
        }
        if !(200..300).contains(&status_code) {
            match status_code {
                _ => {
                    let body = Ok::<Vec<u8>, Error>(body_bytes.to_vec());
                    return Err(GetNodeNameError::Unexpected(ApiError::new(status_code, response_headers.clone(), body_bytes.to_vec(), body)));
                }
            }
        }
        Ok(result)
    }
}

/// Response from `accept_node`.
#[derive(Debug)]
pub struct AcceptNodeResponse {
    pub status_code: u16,
    pub headers: aioduct::HeaderMap,
    pub data: Option<crate::models::NodeStatus>,
}

/// Error from `accept_node`.
#[derive(Debug)]
pub enum AcceptNodeError {
    Unexpected(ApiError<Vec<u8>>),
    Transport(Error),
}

impl From<Error> for AcceptNodeError {
    fn from(error: Error) -> Self {
        Self::Transport(error)
    }
}

impl std::fmt::Display for AcceptNodeError {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        match self {
            Self::Unexpected(err) => write!(f, "unexpected HTTP error {}", err.status_code()),
            Self::Transport(err) => std::fmt::Display::fmt(err, f),
        }
    }
}

impl std::error::Error for AcceptNodeError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match self {
            Self::Transport(err) => Some(err),
            _ => None,
        }
    }
}

/// Response from `get_node_name`.
#[derive(Debug)]
pub struct GetNodeNameResponse {
    pub status_code: u16,
    pub headers: aioduct::HeaderMap,
    pub data: Option<String>,
}

/// Error from `get_node_name`.
#[derive(Debug)]
pub enum GetNodeNameError {
    Unexpected(ApiError<Vec<u8>>),
    Transport(Error),
}

impl From<Error> for GetNodeNameError {
    fn from(error: Error) -> Self {
        Self::Transport(error)
    }
}

impl std::fmt::Display for GetNodeNameError {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        match self {
            Self::Unexpected(err) => write!(f, "unexpected HTTP error {}", err.status_code()),
            Self::Transport(err) => std::fmt::Display::fmt(err, f),
        }
    }
}

impl std::error::Error for GetNodeNameError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match self {
            Self::Transport(err) => Some(err),
            _ => None,
        }
    }
}
