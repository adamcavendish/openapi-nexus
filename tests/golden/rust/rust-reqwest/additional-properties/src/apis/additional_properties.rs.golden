// @generated
// Code generated by openapi-nexus. DO NOT EDIT.
//
// Additional Properties API — 1.0.0
// API demonstrating OpenAPI additionalProperties with multiple levels of structs (RootLevel -> MiddleLevel -> LeafValue), each with HashMap fields.

use crate::runtime::client::Client;
use crate::runtime::error::{ApiError, Error};

/// API operations under the "additional-properties" tag.
pub struct AdditionalPropertiesApi<'a> {
    client: &'a Client,
}

impl<'a> AdditionalPropertiesApi<'a> {
    /// Create a new `AdditionalPropertiesApi` bound to the given client.
    pub fn new(client: &'a Client) -> Self {
        Self {
            client,
        }
    }

    /// Echo leaf payload (attributes map).
    pub async fn post_leaf(
        &self,
        body: &crate::models::LeafValue,
    ) -> Result<PostLeafResponse, PostLeafError> {
        let path = "/leaf".to_string();
        let mut req = self.client.request(reqwest::Method::POST, &path).await?;
        req = req.json(&body);
        let resp = self.client.send(req).await?;
        let status_code = resp.status().as_u16();
        let response_headers = resp.headers().clone();
        let body_bytes = resp.bytes().await.map_err(Error::Network)?;
        let mut result = PostLeafResponse { status_code, headers: response_headers.clone(), data: None };
        match status_code {
            200 => {
                result.data = Some(serde_json::from_slice(&body_bytes).map_err(Error::Deserialize)?);
            }
            _ => {}
        }
        if !(200..300).contains(&status_code) {
            match status_code {
                400 => {
                    let body = Ok::<(), Error>(());
                    return Err(PostLeafError::BadRequest(ApiError::new(status_code, response_headers.clone(), body_bytes.to_vec(), body)));
                }
                _ => {
                    let body = Ok::<Vec<u8>, Error>(body_bytes.to_vec());
                    return Err(PostLeafError::Unexpected(ApiError::new(status_code, response_headers.clone(), body_bytes.to_vec(), body)));
                }
            }
        }
        Ok(result)
    }

    /// Echo middle-level payload (nested + extra maps).
    pub async fn post_middle(
        &self,
        body: &crate::models::MiddleLevel,
    ) -> Result<PostMiddleResponse, PostMiddleError> {
        let path = "/middle".to_string();
        let mut req = self.client.request(reqwest::Method::POST, &path).await?;
        req = req.json(&body);
        let resp = self.client.send(req).await?;
        let status_code = resp.status().as_u16();
        let response_headers = resp.headers().clone();
        let body_bytes = resp.bytes().await.map_err(Error::Network)?;
        let mut result = PostMiddleResponse { status_code, headers: response_headers.clone(), data: None };
        match status_code {
            200 => {
                result.data = Some(serde_json::from_slice(&body_bytes).map_err(Error::Deserialize)?);
            }
            _ => {}
        }
        if !(200..300).contains(&status_code) {
            match status_code {
                400 => {
                    let body = Ok::<(), Error>(());
                    return Err(PostMiddleError::BadRequest(ApiError::new(status_code, response_headers.clone(), body_bytes.to_vec(), body)));
                }
                _ => {
                    let body = Ok::<Vec<u8>, Error>(body_bytes.to_vec());
                    return Err(PostMiddleError::Unexpected(ApiError::new(status_code, response_headers.clone(), body_bytes.to_vec(), body)));
                }
            }
        }
        Ok(result)
    }

    /// Echo root payload (all three levels with additional-properties-style maps).
    pub async fn post_root(
        &self,
        body: &crate::models::RootLevel,
    ) -> Result<PostRootResponse, PostRootError> {
        let path = "/root".to_string();
        let mut req = self.client.request(reqwest::Method::POST, &path).await?;
        req = req.json(&body);
        let resp = self.client.send(req).await?;
        let status_code = resp.status().as_u16();
        let response_headers = resp.headers().clone();
        let body_bytes = resp.bytes().await.map_err(Error::Network)?;
        let mut result = PostRootResponse { status_code, headers: response_headers.clone(), data: None };
        match status_code {
            200 => {
                result.data = Some(serde_json::from_slice(&body_bytes).map_err(Error::Deserialize)?);
            }
            _ => {}
        }
        if !(200..300).contains(&status_code) {
            match status_code {
                400 => {
                    let body = Ok::<(), Error>(());
                    return Err(PostRootError::BadRequest(ApiError::new(status_code, response_headers.clone(), body_bytes.to_vec(), body)));
                }
                _ => {
                    let body = Ok::<Vec<u8>, Error>(body_bytes.to_vec());
                    return Err(PostRootError::Unexpected(ApiError::new(status_code, response_headers.clone(), body_bytes.to_vec(), body)));
                }
            }
        }
        Ok(result)
    }
}

/// Response from `post_leaf`.
#[derive(Debug)]
pub struct PostLeafResponse {
    pub status_code: u16,
    pub headers: reqwest::header::HeaderMap,
    pub data: Option<crate::models::LeafValue>,
}

/// Error from `post_leaf`.
#[derive(Debug)]
pub enum PostLeafError {
    BadRequest(ApiError<()>),
    Unexpected(ApiError<Vec<u8>>),
    Transport(Error),
}

impl From<Error> for PostLeafError {
    fn from(error: Error) -> Self {
        Self::Transport(error)
    }
}

impl std::fmt::Display for PostLeafError {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        match self {
            Self::BadRequest(err) => write!(f, "HTTP error {}", err.status_code()),
            Self::Unexpected(err) => write!(f, "unexpected HTTP error {}", err.status_code()),
            Self::Transport(err) => std::fmt::Display::fmt(err, f),
        }
    }
}

impl std::error::Error for PostLeafError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match self {
            Self::Transport(err) => Some(err),
            _ => None,
        }
    }
}

/// Response from `post_middle`.
#[derive(Debug)]
pub struct PostMiddleResponse {
    pub status_code: u16,
    pub headers: reqwest::header::HeaderMap,
    pub data: Option<crate::models::MiddleLevel>,
}

/// Error from `post_middle`.
#[derive(Debug)]
pub enum PostMiddleError {
    BadRequest(ApiError<()>),
    Unexpected(ApiError<Vec<u8>>),
    Transport(Error),
}

impl From<Error> for PostMiddleError {
    fn from(error: Error) -> Self {
        Self::Transport(error)
    }
}

impl std::fmt::Display for PostMiddleError {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        match self {
            Self::BadRequest(err) => write!(f, "HTTP error {}", err.status_code()),
            Self::Unexpected(err) => write!(f, "unexpected HTTP error {}", err.status_code()),
            Self::Transport(err) => std::fmt::Display::fmt(err, f),
        }
    }
}

impl std::error::Error for PostMiddleError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match self {
            Self::Transport(err) => Some(err),
            _ => None,
        }
    }
}

/// Response from `post_root`.
#[derive(Debug)]
pub struct PostRootResponse {
    pub status_code: u16,
    pub headers: reqwest::header::HeaderMap,
    pub data: Option<crate::models::RootLevel>,
}

/// Error from `post_root`.
#[derive(Debug)]
pub enum PostRootError {
    BadRequest(ApiError<()>),
    Unexpected(ApiError<Vec<u8>>),
    Transport(Error),
}

impl From<Error> for PostRootError {
    fn from(error: Error) -> Self {
        Self::Transport(error)
    }
}

impl std::fmt::Display for PostRootError {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        match self {
            Self::BadRequest(err) => write!(f, "HTTP error {}", err.status_code()),
            Self::Unexpected(err) => write!(f, "unexpected HTTP error {}", err.status_code()),
            Self::Transport(err) => std::fmt::Display::fmt(err, f),
        }
    }
}

impl std::error::Error for PostRootError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match self {
            Self::Transport(err) => Some(err),
            _ => None,
        }
    }
}
