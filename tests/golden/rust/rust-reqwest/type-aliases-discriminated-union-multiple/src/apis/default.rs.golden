// @generated
// Code generated by openapi-nexus. DO NOT EDIT.
//
// Multiple Discriminated Unions Test — 1.0.0
// Test fixture with multiple discriminated unions to verify Kind types
// are properly namespaced and don't conflict.
// This reproduces the original bug where both unions would generate
// the same Kind.ts file causing conflicts.

use crate::runtime::client::Client;
use crate::runtime::error::{ApiCallError, ApiError, Error};

/// API operations under the "default" tag.
pub struct DefaultApi<'a> {
    client: &'a Client,
}

impl<'a> DefaultApi<'a> {
    /// Create a new `DefaultApi` bound to the given client.
    pub fn new(client: &'a Client) -> Self {
        Self {
            client,
        }
    }

    /// Create container with ContainerKind discriminated union
    pub async fn create_container(
        &self,
        body: &crate::models::ContainerKind,
    ) -> Result<CreateContainerResponse, CreateContainerError> {
        let path = "/container".to_string();
        let mut req = self.client.request(reqwest::Method::POST, &path).await?;
        req = req.json(&body);
        let resp = self.client.send(req).await?;
        let status_code = resp.status().as_u16();
        let response_headers = resp.headers().clone();
        let body_bytes = resp.bytes().await.map_err(Error::Network)?;
        let mut result = CreateContainerResponse { status_code, headers: response_headers.clone(), data: None };
        match status_code {
            200 => {
                result.data = Some(serde_json::from_slice(&body_bytes).map_err(Error::Deserialize)?);
            }
            _ => {}
        }
        if !(200..300).contains(&status_code) {
            match status_code {
                _ => {
                    let body = Ok::<Vec<u8>, Error>(body_bytes.to_vec());
                    return Err(CreateContainerError::Unexpected(ApiError::new(status_code, response_headers.clone(), body_bytes.to_vec(), body)));
                }
            }
        }
        Ok(result)
    }

    /// Create volume with VolumeKind discriminated union
    pub async fn create_volume(
        &self,
        body: &crate::models::VolumeKind,
    ) -> Result<CreateVolumeResponse, CreateVolumeError> {
        let path = "/volume".to_string();
        let mut req = self.client.request(reqwest::Method::POST, &path).await?;
        req = req.json(&body);
        let resp = self.client.send(req).await?;
        let status_code = resp.status().as_u16();
        let response_headers = resp.headers().clone();
        let body_bytes = resp.bytes().await.map_err(Error::Network)?;
        let mut result = CreateVolumeResponse { status_code, headers: response_headers.clone(), data: None };
        match status_code {
            200 => {
                result.data = Some(serde_json::from_slice(&body_bytes).map_err(Error::Deserialize)?);
            }
            _ => {}
        }
        if !(200..300).contains(&status_code) {
            match status_code {
                _ => {
                    let body = Ok::<Vec<u8>, Error>(body_bytes.to_vec());
                    return Err(CreateVolumeError::Unexpected(ApiError::new(status_code, response_headers.clone(), body_bytes.to_vec(), body)));
                }
            }
        }
        Ok(result)
    }
}

/// Response from `create_container`.
#[derive(Debug)]
pub struct CreateContainerResponse {
    pub status_code: u16,
    pub headers: reqwest::header::HeaderMap,
    pub data: Option<crate::models::ContainerKind>,
}

/// Error from `create_container`.
#[derive(Debug)]
pub enum CreateContainerError {
    Unexpected(ApiError<Vec<u8>>),
    Transport(Error),
}

impl From<Error> for CreateContainerError {
    fn from(error: Error) -> Self {
        Self::Transport(error)
    }
}

impl From<CreateContainerError> for ApiCallError {
    fn from(error: CreateContainerError) -> Self {
        match error {
            CreateContainerError::Unexpected(error) => Self::from_api_error("create_container", error),
            CreateContainerError::Transport(error) => Self::from_runtime_error("create_container", error),
        }
    }
}

impl std::fmt::Display for CreateContainerError {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        match self {
            Self::Unexpected(err) => write!(f, "unexpected HTTP error {}", err.status_code()),
            Self::Transport(err) => std::fmt::Display::fmt(err, f),
        }
    }
}

impl std::error::Error for CreateContainerError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match self {
            Self::Transport(err) => Some(err),
            _ => None,
        }
    }
}

/// Response from `create_volume`.
#[derive(Debug)]
pub struct CreateVolumeResponse {
    pub status_code: u16,
    pub headers: reqwest::header::HeaderMap,
    pub data: Option<crate::models::VolumeKind>,
}

/// Error from `create_volume`.
#[derive(Debug)]
pub enum CreateVolumeError {
    Unexpected(ApiError<Vec<u8>>),
    Transport(Error),
}

impl From<Error> for CreateVolumeError {
    fn from(error: Error) -> Self {
        Self::Transport(error)
    }
}

impl From<CreateVolumeError> for ApiCallError {
    fn from(error: CreateVolumeError) -> Self {
        match error {
            CreateVolumeError::Unexpected(error) => Self::from_api_error("create_volume", error),
            CreateVolumeError::Transport(error) => Self::from_runtime_error("create_volume", error),
        }
    }
}

impl std::fmt::Display for CreateVolumeError {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        match self {
            Self::Unexpected(err) => write!(f, "unexpected HTTP error {}", err.status_code()),
            Self::Transport(err) => std::fmt::Display::fmt(err, f),
        }
    }
}

impl std::error::Error for CreateVolumeError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match self {
            Self::Transport(err) => Some(err),
            _ => None,
        }
    }
}
