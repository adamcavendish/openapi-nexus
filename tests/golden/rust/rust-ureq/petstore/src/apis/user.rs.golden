// @generated
// Code generated by openapi-nexus. DO NOT EDIT.
//
// Petstore API — 1.0.0
// This is a sample Pet Store Server based on the OpenAPI 3.1 specification

use crate::runtime::client::Client;
use crate::runtime::error::{ApiCallError, ApiError, Error};

/// API operations under the "user" tag.
pub struct UserApi<'a> {
    client: &'a Client,
}

impl<'a> UserApi<'a> {
    /// Create a new `UserApi` bound to the given client.
    pub fn new(client: &'a Client) -> Self {
        Self {
            client,
        }
    }

    /// Create user
    pub fn create_user(
        &self,
        body: &crate::models::User,
    ) -> Result<CreateUserResponse, CreateUserError> {
        let path = "/user".to_string();
        let req = self.client.post(&path);
        let resp = req.send_json(&body).map_err(Error::Http)?;
        let status_code = resp.status().as_u16();
        let response_headers = resp.headers().clone();
        let body_bytes = resp.into_body().read_to_vec().map_err(Error::Http)?;
        let mut result = CreateUserResponse { status_code, headers: response_headers.clone(), data: None };
        match status_code {
            200 => {
                result.data = Some(serde_json::from_slice(body_bytes.as_slice()).map_err(Error::Deserialize)?);
            }
            _ => {}
        }
        if !(200..300).contains(&status_code) {
            match status_code {
                _ => {
                    let body = Ok::<Vec<u8>, Error>(body_bytes.to_vec());
                    return Err(CreateUserError::Unexpected(ApiError::new(status_code, response_headers.clone(), body_bytes.to_vec(), body)));
                }
            }
        }
        Ok(result)
    }

    /// Creates list of users with given input array
    pub fn create_users_with_list_input(
        &self,
        body: &Vec<crate::models::User>,
    ) -> Result<CreateUsersWithListInputResponse, CreateUsersWithListInputError> {
        let path = "/user/createWithList".to_string();
        let req = self.client.post(&path);
        let resp = req.send_json(&body).map_err(Error::Http)?;
        let status_code = resp.status().as_u16();
        let response_headers = resp.headers().clone();
        let body_bytes = resp.into_body().read_to_vec().map_err(Error::Http)?;
        let mut result = CreateUsersWithListInputResponse { status_code, headers: response_headers.clone(), data: None };
        match status_code {
            200 => {
                result.data = Some(serde_json::from_slice(body_bytes.as_slice()).map_err(Error::Deserialize)?);
            }
            _ => {}
        }
        if !(200..300).contains(&status_code) {
            match status_code {
                _ => {
                    let body = Ok::<Vec<u8>, Error>(body_bytes.to_vec());
                    return Err(CreateUsersWithListInputError::Unexpected(ApiError::new(status_code, response_headers.clone(), body_bytes.to_vec(), body)));
                }
            }
        }
        Ok(result)
    }

    /// Logs user into the system
    pub fn login_user(
        &self,
        username: &Option<String>,
        password: &Option<String>,
    ) -> Result<LoginUserResponse, LoginUserError> {
        let path = "/user/login".to_string();
        let mut req = self.client.get(&path);
        if let Some(v) = &username {
            req = req.query("username", &v.to_string());
        }
        if let Some(v) = &password {
            req = req.query("password", &v.to_string());
        }
        let resp = req.call().map_err(Error::Http)?;
        let status_code = resp.status().as_u16();
        let response_headers = resp.headers().clone();
        let result = LoginUserResponse { status_code, headers: response_headers.clone() };
        if !(200..300).contains(&status_code) {
            let body_bytes = resp.into_body().read_to_vec().map_err(Error::Http)?;
            if !(200..300).contains(&status_code) {
                match status_code {
                    400 => {
                        let body = Ok::<(), Error>(());
                        return Err(LoginUserError::BadRequest(ApiError::new(status_code, response_headers.clone(), body_bytes.to_vec(), body)));
                    }
                    _ => {
                        let body = Ok::<Vec<u8>, Error>(body_bytes.to_vec());
                        return Err(LoginUserError::Unexpected(ApiError::new(status_code, response_headers.clone(), body_bytes.to_vec(), body)));
                    }
                }
            }
        }
        Ok(result)
    }

    /// Logs out current logged in user session
    pub fn logout_user(
        &self,
    ) -> Result<LogoutUserResponse, LogoutUserError> {
        let path = "/user/logout".to_string();
        let req = self.client.get(&path);
        let resp = req.call().map_err(Error::Http)?;
        let status_code = resp.status().as_u16();
        let response_headers = resp.headers().clone();
        let result = LogoutUserResponse { status_code, headers: response_headers.clone() };
        if !(200..300).contains(&status_code) {
            let body_bytes = resp.into_body().read_to_vec().map_err(Error::Http)?;
            if !(200..300).contains(&status_code) {
                match status_code {
                    _ => {
                        let body = Ok::<Vec<u8>, Error>(body_bytes.to_vec());
                        return Err(LogoutUserError::Unexpected(ApiError::new(status_code, response_headers.clone(), body_bytes.to_vec(), body)));
                    }
                }
            }
        }
        Ok(result)
    }

    /// Get user by user name
    pub fn get_user_by_name(
        &self,
        username: &str,
    ) -> Result<GetUserByNameResponse, GetUserByNameError> {
        let mut path = "/user/{username}".to_string();
        path = path.replace("{username}", &username.to_string());
        let req = self.client.get(&path);
        let resp = req.call().map_err(Error::Http)?;
        let status_code = resp.status().as_u16();
        let response_headers = resp.headers().clone();
        let body_bytes = resp.into_body().read_to_vec().map_err(Error::Http)?;
        let mut result = GetUserByNameResponse { status_code, headers: response_headers.clone(), data: None };
        match status_code {
            200 => {
                result.data = Some(serde_json::from_slice(body_bytes.as_slice()).map_err(Error::Deserialize)?);
            }
            _ => {}
        }
        if !(200..300).contains(&status_code) {
            match status_code {
                400 => {
                    let body = Ok::<(), Error>(());
                    return Err(GetUserByNameError::BadRequest(ApiError::new(status_code, response_headers.clone(), body_bytes.to_vec(), body)));
                }
                404 => {
                    let body = Ok::<(), Error>(());
                    return Err(GetUserByNameError::NotFound(ApiError::new(status_code, response_headers.clone(), body_bytes.to_vec(), body)));
                }
                _ => {
                    let body = Ok::<Vec<u8>, Error>(body_bytes.to_vec());
                    return Err(GetUserByNameError::Unexpected(ApiError::new(status_code, response_headers.clone(), body_bytes.to_vec(), body)));
                }
            }
        }
        Ok(result)
    }

    /// Update user
    pub fn update_user(
        &self,
        username: &str,
        body: &crate::models::User,
    ) -> Result<UpdateUserResponse, UpdateUserError> {
        let mut path = "/user/{username}".to_string();
        path = path.replace("{username}", &username.to_string());
        let req = self.client.put(&path);
        let resp = req.send_json(&body).map_err(Error::Http)?;
        let status_code = resp.status().as_u16();
        let response_headers = resp.headers().clone();
        let result = UpdateUserResponse { status_code, headers: response_headers.clone() };
        if !(200..300).contains(&status_code) {
            let body_bytes = resp.into_body().read_to_vec().map_err(Error::Http)?;
            if !(200..300).contains(&status_code) {
                match status_code {
                    400 => {
                        let body = Ok::<(), Error>(());
                        return Err(UpdateUserError::BadRequest(ApiError::new(status_code, response_headers.clone(), body_bytes.to_vec(), body)));
                    }
                    404 => {
                        let body = Ok::<(), Error>(());
                        return Err(UpdateUserError::NotFound(ApiError::new(status_code, response_headers.clone(), body_bytes.to_vec(), body)));
                    }
                    _ => {
                        let body = Ok::<Vec<u8>, Error>(body_bytes.to_vec());
                        return Err(UpdateUserError::Unexpected(ApiError::new(status_code, response_headers.clone(), body_bytes.to_vec(), body)));
                    }
                }
            }
        }
        Ok(result)
    }

    /// Delete user
    pub fn delete_user(
        &self,
        username: &str,
    ) -> Result<DeleteUserResponse, DeleteUserError> {
        let mut path = "/user/{username}".to_string();
        path = path.replace("{username}", &username.to_string());
        let req = self.client.delete(&path);
        let resp = req.call().map_err(Error::Http)?;
        let status_code = resp.status().as_u16();
        let response_headers = resp.headers().clone();
        let result = DeleteUserResponse { status_code, headers: response_headers.clone() };
        if !(200..300).contains(&status_code) {
            let body_bytes = resp.into_body().read_to_vec().map_err(Error::Http)?;
            if !(200..300).contains(&status_code) {
                match status_code {
                    400 => {
                        let body = Ok::<(), Error>(());
                        return Err(DeleteUserError::BadRequest(ApiError::new(status_code, response_headers.clone(), body_bytes.to_vec(), body)));
                    }
                    404 => {
                        let body = Ok::<(), Error>(());
                        return Err(DeleteUserError::NotFound(ApiError::new(status_code, response_headers.clone(), body_bytes.to_vec(), body)));
                    }
                    _ => {
                        let body = Ok::<Vec<u8>, Error>(body_bytes.to_vec());
                        return Err(DeleteUserError::Unexpected(ApiError::new(status_code, response_headers.clone(), body_bytes.to_vec(), body)));
                    }
                }
            }
        }
        Ok(result)
    }
}

/// Response from `create_user`.
#[derive(Debug)]
pub struct CreateUserResponse {
    pub status_code: u16,
    pub headers: ureq::http::HeaderMap,
    pub data: Option<crate::models::User>,
}

/// Error from `create_user`.
#[derive(Debug)]
pub enum CreateUserError {
    Unexpected(ApiError<Vec<u8>>),
    Transport(Error),
}

impl From<Error> for CreateUserError {
    fn from(error: Error) -> Self {
        Self::Transport(error)
    }
}

impl From<CreateUserError> for ApiCallError {
    fn from(error: CreateUserError) -> Self {
        match error {
            CreateUserError::Unexpected(error) => Self::from_api_error("create_user", error),
            CreateUserError::Transport(error) => Self::from_runtime_error("create_user", error),
        }
    }
}

impl std::fmt::Display for CreateUserError {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        match self {
            Self::Unexpected(err) => write!(f, "unexpected HTTP error {}", err.status_code()),
            Self::Transport(err) => std::fmt::Display::fmt(err, f),
        }
    }
}

impl std::error::Error for CreateUserError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match self {
            Self::Transport(err) => Some(err),
            _ => None,
        }
    }
}

/// Response from `create_users_with_list_input`.
#[derive(Debug)]
pub struct CreateUsersWithListInputResponse {
    pub status_code: u16,
    pub headers: ureq::http::HeaderMap,
    pub data: Option<crate::models::User>,
}

/// Error from `create_users_with_list_input`.
#[derive(Debug)]
pub enum CreateUsersWithListInputError {
    Unexpected(ApiError<Vec<u8>>),
    Transport(Error),
}

impl From<Error> for CreateUsersWithListInputError {
    fn from(error: Error) -> Self {
        Self::Transport(error)
    }
}

impl From<CreateUsersWithListInputError> for ApiCallError {
    fn from(error: CreateUsersWithListInputError) -> Self {
        match error {
            CreateUsersWithListInputError::Unexpected(error) => Self::from_api_error("create_users_with_list_input", error),
            CreateUsersWithListInputError::Transport(error) => Self::from_runtime_error("create_users_with_list_input", error),
        }
    }
}

impl std::fmt::Display for CreateUsersWithListInputError {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        match self {
            Self::Unexpected(err) => write!(f, "unexpected HTTP error {}", err.status_code()),
            Self::Transport(err) => std::fmt::Display::fmt(err, f),
        }
    }
}

impl std::error::Error for CreateUsersWithListInputError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match self {
            Self::Transport(err) => Some(err),
            _ => None,
        }
    }
}

/// Response from `login_user`.
#[derive(Debug)]
pub struct LoginUserResponse {
    pub status_code: u16,
    pub headers: ureq::http::HeaderMap,
}

/// Error from `login_user`.
#[derive(Debug)]
pub enum LoginUserError {
    BadRequest(ApiError<()>),
    Unexpected(ApiError<Vec<u8>>),
    Transport(Error),
}

impl From<Error> for LoginUserError {
    fn from(error: Error) -> Self {
        Self::Transport(error)
    }
}

impl From<LoginUserError> for ApiCallError {
    fn from(error: LoginUserError) -> Self {
        match error {
            LoginUserError::BadRequest(error) => Self::from_api_error("login_user", error),
            LoginUserError::Unexpected(error) => Self::from_api_error("login_user", error),
            LoginUserError::Transport(error) => Self::from_runtime_error("login_user", error),
        }
    }
}

impl std::fmt::Display for LoginUserError {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        match self {
            Self::BadRequest(err) => write!(f, "HTTP error {}", err.status_code()),
            Self::Unexpected(err) => write!(f, "unexpected HTTP error {}", err.status_code()),
            Self::Transport(err) => std::fmt::Display::fmt(err, f),
        }
    }
}

impl std::error::Error for LoginUserError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match self {
            Self::Transport(err) => Some(err),
            _ => None,
        }
    }
}

/// Response from `logout_user`.
#[derive(Debug)]
pub struct LogoutUserResponse {
    pub status_code: u16,
    pub headers: ureq::http::HeaderMap,
}

/// Error from `logout_user`.
#[derive(Debug)]
pub enum LogoutUserError {
    Unexpected(ApiError<Vec<u8>>),
    Transport(Error),
}

impl From<Error> for LogoutUserError {
    fn from(error: Error) -> Self {
        Self::Transport(error)
    }
}

impl From<LogoutUserError> for ApiCallError {
    fn from(error: LogoutUserError) -> Self {
        match error {
            LogoutUserError::Unexpected(error) => Self::from_api_error("logout_user", error),
            LogoutUserError::Transport(error) => Self::from_runtime_error("logout_user", error),
        }
    }
}

impl std::fmt::Display for LogoutUserError {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        match self {
            Self::Unexpected(err) => write!(f, "unexpected HTTP error {}", err.status_code()),
            Self::Transport(err) => std::fmt::Display::fmt(err, f),
        }
    }
}

impl std::error::Error for LogoutUserError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match self {
            Self::Transport(err) => Some(err),
            _ => None,
        }
    }
}

/// Response from `get_user_by_name`.
#[derive(Debug)]
pub struct GetUserByNameResponse {
    pub status_code: u16,
    pub headers: ureq::http::HeaderMap,
    pub data: Option<crate::models::User>,
}

/// Error from `get_user_by_name`.
#[derive(Debug)]
pub enum GetUserByNameError {
    BadRequest(ApiError<()>),
    NotFound(ApiError<()>),
    Unexpected(ApiError<Vec<u8>>),
    Transport(Error),
}

impl From<Error> for GetUserByNameError {
    fn from(error: Error) -> Self {
        Self::Transport(error)
    }
}

impl From<GetUserByNameError> for ApiCallError {
    fn from(error: GetUserByNameError) -> Self {
        match error {
            GetUserByNameError::BadRequest(error) => Self::from_api_error("get_user_by_name", error),
            GetUserByNameError::NotFound(error) => Self::from_api_error("get_user_by_name", error),
            GetUserByNameError::Unexpected(error) => Self::from_api_error("get_user_by_name", error),
            GetUserByNameError::Transport(error) => Self::from_runtime_error("get_user_by_name", error),
        }
    }
}

impl std::fmt::Display for GetUserByNameError {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        match self {
            Self::BadRequest(err) => write!(f, "HTTP error {}", err.status_code()),
            Self::NotFound(err) => write!(f, "HTTP error {}", err.status_code()),
            Self::Unexpected(err) => write!(f, "unexpected HTTP error {}", err.status_code()),
            Self::Transport(err) => std::fmt::Display::fmt(err, f),
        }
    }
}

impl std::error::Error for GetUserByNameError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match self {
            Self::Transport(err) => Some(err),
            _ => None,
        }
    }
}

/// Response from `update_user`.
#[derive(Debug)]
pub struct UpdateUserResponse {
    pub status_code: u16,
    pub headers: ureq::http::HeaderMap,
}

/// Error from `update_user`.
#[derive(Debug)]
pub enum UpdateUserError {
    BadRequest(ApiError<()>),
    NotFound(ApiError<()>),
    Unexpected(ApiError<Vec<u8>>),
    Transport(Error),
}

impl From<Error> for UpdateUserError {
    fn from(error: Error) -> Self {
        Self::Transport(error)
    }
}

impl From<UpdateUserError> for ApiCallError {
    fn from(error: UpdateUserError) -> Self {
        match error {
            UpdateUserError::BadRequest(error) => Self::from_api_error("update_user", error),
            UpdateUserError::NotFound(error) => Self::from_api_error("update_user", error),
            UpdateUserError::Unexpected(error) => Self::from_api_error("update_user", error),
            UpdateUserError::Transport(error) => Self::from_runtime_error("update_user", error),
        }
    }
}

impl std::fmt::Display for UpdateUserError {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        match self {
            Self::BadRequest(err) => write!(f, "HTTP error {}", err.status_code()),
            Self::NotFound(err) => write!(f, "HTTP error {}", err.status_code()),
            Self::Unexpected(err) => write!(f, "unexpected HTTP error {}", err.status_code()),
            Self::Transport(err) => std::fmt::Display::fmt(err, f),
        }
    }
}

impl std::error::Error for UpdateUserError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match self {
            Self::Transport(err) => Some(err),
            _ => None,
        }
    }
}

/// Response from `delete_user`.
#[derive(Debug)]
pub struct DeleteUserResponse {
    pub status_code: u16,
    pub headers: ureq::http::HeaderMap,
}

/// Error from `delete_user`.
#[derive(Debug)]
pub enum DeleteUserError {
    BadRequest(ApiError<()>),
    NotFound(ApiError<()>),
    Unexpected(ApiError<Vec<u8>>),
    Transport(Error),
}

impl From<Error> for DeleteUserError {
    fn from(error: Error) -> Self {
        Self::Transport(error)
    }
}

impl From<DeleteUserError> for ApiCallError {
    fn from(error: DeleteUserError) -> Self {
        match error {
            DeleteUserError::BadRequest(error) => Self::from_api_error("delete_user", error),
            DeleteUserError::NotFound(error) => Self::from_api_error("delete_user", error),
            DeleteUserError::Unexpected(error) => Self::from_api_error("delete_user", error),
            DeleteUserError::Transport(error) => Self::from_runtime_error("delete_user", error),
        }
    }
}

impl std::fmt::Display for DeleteUserError {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        match self {
            Self::BadRequest(err) => write!(f, "HTTP error {}", err.status_code()),
            Self::NotFound(err) => write!(f, "HTTP error {}", err.status_code()),
            Self::Unexpected(err) => write!(f, "unexpected HTTP error {}", err.status_code()),
            Self::Transport(err) => std::fmt::Display::fmt(err, f),
        }
    }
}

impl std::error::Error for DeleteUserError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match self {
            Self::Transport(err) => Some(err),
            _ => None,
        }
    }
}
