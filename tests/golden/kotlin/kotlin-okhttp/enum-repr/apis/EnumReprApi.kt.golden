// Code generated by openapi-nexus. DO NOT EDIT.
//
// Enum Representation API — 1.0.0
// This API demonstrates all 4 kinds of enum representation types: Externally Tagged, Internally Tagged, Adjacently Tagged, and Untagged

package com.example.sdk.apis

import com.example.sdk.models.*
import com.example.sdk.runtime.ApiClient
import com.example.sdk.runtime.ApiException
import com.google.gson.Gson
import com.google.gson.reflect.TypeToken
import okhttp3.MediaType.Companion.toMediaType
import okhttp3.RequestBody.Companion.toRequestBody
import okhttp3.Response

/**
 * HandleAdjacentlyTaggedResponse carries the response from handleAdjacentlyTagged.
 */
data class HandleAdjacentlyTaggedResponse(val statusCode: Int, val raw: Response,
val status200: AdjacentlyTaggedEnum? = null) {
}

sealed interface HandleAdjacentlyTaggedExceptionDetail {
    val statusCode: Int
    val headers: okhttp3.Headers
    fun rawBody(): ByteArray
}

class HandleAdjacentlyTaggedException(
    val detail: HandleAdjacentlyTaggedExceptionDetail,
    statusCode: Int,
    status: String,
    body: String,
    val headers: okhttp3.Headers,
    rawBodyBytes: ByteArray,
) : ApiException(statusCode, status, body) {
    private val rawBodyBytes: ByteArray = rawBodyBytes.copyOf()
    fun rawBody(): ByteArray = this.rawBodyBytes.copyOf()
}

class HandleAdjacentlyTaggedBadRequest(
    override val statusCode: Int,
    override val headers: okhttp3.Headers,
    rawBodyBytes: ByteArray,
) : HandleAdjacentlyTaggedExceptionDetail {
    private val rawBodyBytes: ByteArray = rawBodyBytes.copyOf()
    override fun rawBody(): ByteArray = this.rawBodyBytes.copyOf()
    fun body(): ByteArray = rawBody()
}

class HandleAdjacentlyTaggedUnexpected(
    override val statusCode: Int,
    override val headers: okhttp3.Headers,
    rawBodyBytes: ByteArray,
) : HandleAdjacentlyTaggedExceptionDetail {
    private val rawBodyBytes: ByteArray = rawBodyBytes.copyOf()
    override fun rawBody(): ByteArray = this.rawBodyBytes.copyOf()
    fun body(): ByteArray = rawBody()
}

/**
 * HandleExternallyTaggedResponse carries the response from handleExternallyTagged.
 */
data class HandleExternallyTaggedResponse(val statusCode: Int, val raw: Response,
val status200: ExternallyTaggedEnum? = null) {
}

sealed interface HandleExternallyTaggedExceptionDetail {
    val statusCode: Int
    val headers: okhttp3.Headers
    fun rawBody(): ByteArray
}

class HandleExternallyTaggedException(
    val detail: HandleExternallyTaggedExceptionDetail,
    statusCode: Int,
    status: String,
    body: String,
    val headers: okhttp3.Headers,
    rawBodyBytes: ByteArray,
) : ApiException(statusCode, status, body) {
    private val rawBodyBytes: ByteArray = rawBodyBytes.copyOf()
    fun rawBody(): ByteArray = this.rawBodyBytes.copyOf()
}

class HandleExternallyTaggedBadRequest(
    override val statusCode: Int,
    override val headers: okhttp3.Headers,
    rawBodyBytes: ByteArray,
) : HandleExternallyTaggedExceptionDetail {
    private val rawBodyBytes: ByteArray = rawBodyBytes.copyOf()
    override fun rawBody(): ByteArray = this.rawBodyBytes.copyOf()
    fun body(): ByteArray = rawBody()
}

class HandleExternallyTaggedUnexpected(
    override val statusCode: Int,
    override val headers: okhttp3.Headers,
    rawBodyBytes: ByteArray,
) : HandleExternallyTaggedExceptionDetail {
    private val rawBodyBytes: ByteArray = rawBodyBytes.copyOf()
    override fun rawBody(): ByteArray = this.rawBodyBytes.copyOf()
    fun body(): ByteArray = rawBody()
}

/**
 * HandleInternallyTaggedResponse carries the response from handleInternallyTagged.
 */
data class HandleInternallyTaggedResponse(val statusCode: Int, val raw: Response,
val status200: InternallyTaggedEnum? = null) {
}

sealed interface HandleInternallyTaggedExceptionDetail {
    val statusCode: Int
    val headers: okhttp3.Headers
    fun rawBody(): ByteArray
}

class HandleInternallyTaggedException(
    val detail: HandleInternallyTaggedExceptionDetail,
    statusCode: Int,
    status: String,
    body: String,
    val headers: okhttp3.Headers,
    rawBodyBytes: ByteArray,
) : ApiException(statusCode, status, body) {
    private val rawBodyBytes: ByteArray = rawBodyBytes.copyOf()
    fun rawBody(): ByteArray = this.rawBodyBytes.copyOf()
}

class HandleInternallyTaggedBadRequest(
    override val statusCode: Int,
    override val headers: okhttp3.Headers,
    rawBodyBytes: ByteArray,
) : HandleInternallyTaggedExceptionDetail {
    private val rawBodyBytes: ByteArray = rawBodyBytes.copyOf()
    override fun rawBody(): ByteArray = this.rawBodyBytes.copyOf()
    fun body(): ByteArray = rawBody()
}

class HandleInternallyTaggedUnexpected(
    override val statusCode: Int,
    override val headers: okhttp3.Headers,
    rawBodyBytes: ByteArray,
) : HandleInternallyTaggedExceptionDetail {
    private val rawBodyBytes: ByteArray = rawBodyBytes.copyOf()
    override fun rawBody(): ByteArray = this.rawBodyBytes.copyOf()
    fun body(): ByteArray = rawBody()
}

/**
 * HandleMixedResponse carries the response from handleMixed.
 */
data class HandleMixedResponse(val statusCode: Int, val raw: Response,
val status200: MixedEnum? = null) {
}

sealed interface HandleMixedExceptionDetail {
    val statusCode: Int
    val headers: okhttp3.Headers
    fun rawBody(): ByteArray
}

class HandleMixedException(
    val detail: HandleMixedExceptionDetail,
    statusCode: Int,
    status: String,
    body: String,
    val headers: okhttp3.Headers,
    rawBodyBytes: ByteArray,
) : ApiException(statusCode, status, body) {
    private val rawBodyBytes: ByteArray = rawBodyBytes.copyOf()
    fun rawBody(): ByteArray = this.rawBodyBytes.copyOf()
}

class HandleMixedBadRequest(
    override val statusCode: Int,
    override val headers: okhttp3.Headers,
    rawBodyBytes: ByteArray,
) : HandleMixedExceptionDetail {
    private val rawBodyBytes: ByteArray = rawBodyBytes.copyOf()
    override fun rawBody(): ByteArray = this.rawBodyBytes.copyOf()
    fun body(): ByteArray = rawBody()
}

class HandleMixedUnexpected(
    override val statusCode: Int,
    override val headers: okhttp3.Headers,
    rawBodyBytes: ByteArray,
) : HandleMixedExceptionDetail {
    private val rawBodyBytes: ByteArray = rawBodyBytes.copyOf()
    override fun rawBody(): ByteArray = this.rawBodyBytes.copyOf()
    fun body(): ByteArray = rawBody()
}

/**
 * HandleUntaggedResponse carries the response from handleUntagged.
 */
data class HandleUntaggedResponse(val statusCode: Int, val raw: Response,
val status200: UntaggedEnum? = null) {
}

sealed interface HandleUntaggedExceptionDetail {
    val statusCode: Int
    val headers: okhttp3.Headers
    fun rawBody(): ByteArray
}

class HandleUntaggedException(
    val detail: HandleUntaggedExceptionDetail,
    statusCode: Int,
    status: String,
    body: String,
    val headers: okhttp3.Headers,
    rawBodyBytes: ByteArray,
) : ApiException(statusCode, status, body) {
    private val rawBodyBytes: ByteArray = rawBodyBytes.copyOf()
    fun rawBody(): ByteArray = this.rawBodyBytes.copyOf()
}

class HandleUntaggedBadRequest(
    override val statusCode: Int,
    override val headers: okhttp3.Headers,
    rawBodyBytes: ByteArray,
) : HandleUntaggedExceptionDetail {
    private val rawBodyBytes: ByteArray = rawBodyBytes.copyOf()
    override fun rawBody(): ByteArray = this.rawBodyBytes.copyOf()
    fun body(): ByteArray = rawBody()
}

class HandleUntaggedUnexpected(
    override val statusCode: Int,
    override val headers: okhttp3.Headers,
    rawBodyBytes: ByteArray,
) : HandleUntaggedExceptionDetail {
    private val rawBodyBytes: ByteArray = rawBodyBytes.copyOf()
    override fun rawBody(): ByteArray = this.rawBodyBytes.copyOf()
    fun body(): ByteArray = rawBody()
}

/**
 * EnumReprApi groups operations under the enum-repr tag.
 */
class EnumReprApi(private val client: ApiClient) {
    private val gson: Gson = Gson()

    /**
     * Handle adjacently tagged enum
     */
    fun handleAdjacentlyTagged(body: AdjacentlyTaggedEnum): HandleAdjacentlyTaggedResponse {
        val path = "/adjacently-tagged"
        val jsonBody = gson.toJson(body)
        val requestBody = jsonBody.toRequestBody("application/json".toMediaType())
        val request = client.newRequestWithBody("POST", path, null, requestBody)
        val response = client.execute(request)

        if (!response.isSuccessful) {
            val responseBody = response.body
            val responseCharset = responseBody?.contentType()?.charset(Charsets.UTF_8) ?: Charsets.UTF_8
            val responseBytes = responseBody?.bytes() ?: ByteArray(0)
            val responseText = responseBytes.toString(responseCharset)
            val detail: HandleAdjacentlyTaggedExceptionDetail = when (response.code)  {
                400 -> HandleAdjacentlyTaggedBadRequest(statusCode = response.code, headers = response.headers, rawBodyBytes = responseBytes)
                else ->  {
                    HandleAdjacentlyTaggedUnexpected(statusCode = response.code, headers = response.headers, rawBodyBytes = responseBytes)
                }
            }
            throw HandleAdjacentlyTaggedException(detail = detail, statusCode = response.code, status = response.message, body = responseText, headers = response.headers, rawBodyBytes = responseBytes)
        }

        val responseBytes = response.body?.bytes() ?: ByteArray(0)
        val responseText = responseBytes.toString(Charsets.UTF_8)
        var status200: AdjacentlyTaggedEnum? = null

        if (response.code == 200) {
            status200 = gson.fromJson<AdjacentlyTaggedEnum>(responseText.ifEmpty { "null" }, object : TypeToken<AdjacentlyTaggedEnum>() {}.type)
        }
        return HandleAdjacentlyTaggedResponse(statusCode = response.code, raw = response, status200 = status200)
    }

    /**
     * Handle externally tagged enum
     */
    fun handleExternallyTagged(body: ExternallyTaggedEnum): HandleExternallyTaggedResponse {
        val path = "/externally-tagged"
        val jsonBody = gson.toJson(body)
        val requestBody = jsonBody.toRequestBody("application/json".toMediaType())
        val request = client.newRequestWithBody("POST", path, null, requestBody)
        val response = client.execute(request)

        if (!response.isSuccessful) {
            val responseBody = response.body
            val responseCharset = responseBody?.contentType()?.charset(Charsets.UTF_8) ?: Charsets.UTF_8
            val responseBytes = responseBody?.bytes() ?: ByteArray(0)
            val responseText = responseBytes.toString(responseCharset)
            val detail: HandleExternallyTaggedExceptionDetail = when (response.code)  {
                400 -> HandleExternallyTaggedBadRequest(statusCode = response.code, headers = response.headers, rawBodyBytes = responseBytes)
                else ->  {
                    HandleExternallyTaggedUnexpected(statusCode = response.code, headers = response.headers, rawBodyBytes = responseBytes)
                }
            }
            throw HandleExternallyTaggedException(detail = detail, statusCode = response.code, status = response.message, body = responseText, headers = response.headers, rawBodyBytes = responseBytes)
        }

        val responseBytes = response.body?.bytes() ?: ByteArray(0)
        val responseText = responseBytes.toString(Charsets.UTF_8)
        var status200: ExternallyTaggedEnum? = null

        if (response.code == 200) {
            status200 = gson.fromJson<ExternallyTaggedEnum>(responseText.ifEmpty { "null" }, object : TypeToken<ExternallyTaggedEnum>() {}.type)
        }
        return HandleExternallyTaggedResponse(statusCode = response.code, raw = response, status200 = status200)
    }

    /**
     * Handle internally tagged enum
     */
    fun handleInternallyTagged(body: InternallyTaggedEnum): HandleInternallyTaggedResponse {
        val path = "/internally-tagged"
        val jsonBody = gson.toJson(body)
        val requestBody = jsonBody.toRequestBody("application/json".toMediaType())
        val request = client.newRequestWithBody("POST", path, null, requestBody)
        val response = client.execute(request)

        if (!response.isSuccessful) {
            val responseBody = response.body
            val responseCharset = responseBody?.contentType()?.charset(Charsets.UTF_8) ?: Charsets.UTF_8
            val responseBytes = responseBody?.bytes() ?: ByteArray(0)
            val responseText = responseBytes.toString(responseCharset)
            val detail: HandleInternallyTaggedExceptionDetail = when (response.code)  {
                400 -> HandleInternallyTaggedBadRequest(statusCode = response.code, headers = response.headers, rawBodyBytes = responseBytes)
                else ->  {
                    HandleInternallyTaggedUnexpected(statusCode = response.code, headers = response.headers, rawBodyBytes = responseBytes)
                }
            }
            throw HandleInternallyTaggedException(detail = detail, statusCode = response.code, status = response.message, body = responseText, headers = response.headers, rawBodyBytes = responseBytes)
        }

        val responseBytes = response.body?.bytes() ?: ByteArray(0)
        val responseText = responseBytes.toString(Charsets.UTF_8)
        var status200: InternallyTaggedEnum? = null

        if (response.code == 200) {
            status200 = gson.fromJson<InternallyTaggedEnum>(responseText.ifEmpty { "null" }, object : TypeToken<InternallyTaggedEnum>() {}.type)
        }
        return HandleInternallyTaggedResponse(statusCode = response.code, raw = response, status200 = status200)
    }

    /**
     * Handle mixed enum (unit variants SimpleA, SimpleB and tuple variants VariantA(VariantA), VariantB(VariantB))
     */
    fun handleMixed(body: MixedEnum): HandleMixedResponse {
        val path = "/mixed"
        val jsonBody = gson.toJson(body)
        val requestBody = jsonBody.toRequestBody("application/json".toMediaType())
        val request = client.newRequestWithBody("POST", path, null, requestBody)
        val response = client.execute(request)

        if (!response.isSuccessful) {
            val responseBody = response.body
            val responseCharset = responseBody?.contentType()?.charset(Charsets.UTF_8) ?: Charsets.UTF_8
            val responseBytes = responseBody?.bytes() ?: ByteArray(0)
            val responseText = responseBytes.toString(responseCharset)
            val detail: HandleMixedExceptionDetail = when (response.code)  {
                400 -> HandleMixedBadRequest(statusCode = response.code, headers = response.headers, rawBodyBytes = responseBytes)
                else ->  {
                    HandleMixedUnexpected(statusCode = response.code, headers = response.headers, rawBodyBytes = responseBytes)
                }
            }
            throw HandleMixedException(detail = detail, statusCode = response.code, status = response.message, body = responseText, headers = response.headers, rawBodyBytes = responseBytes)
        }

        val responseBytes = response.body?.bytes() ?: ByteArray(0)
        val responseText = responseBytes.toString(Charsets.UTF_8)
        var status200: MixedEnum? = null

        if (response.code == 200) {
            status200 = gson.fromJson<MixedEnum>(responseText.ifEmpty { "null" }, object : TypeToken<MixedEnum>() {}.type)
        }
        return HandleMixedResponse(statusCode = response.code, raw = response, status200 = status200)
    }

    /**
     * Handle untagged enum
     */
    fun handleUntagged(body: UntaggedEnum): HandleUntaggedResponse {
        val path = "/untagged"
        val jsonBody = gson.toJson(body)
        val requestBody = jsonBody.toRequestBody("application/json".toMediaType())
        val request = client.newRequestWithBody("POST", path, null, requestBody)
        val response = client.execute(request)

        if (!response.isSuccessful) {
            val responseBody = response.body
            val responseCharset = responseBody?.contentType()?.charset(Charsets.UTF_8) ?: Charsets.UTF_8
            val responseBytes = responseBody?.bytes() ?: ByteArray(0)
            val responseText = responseBytes.toString(responseCharset)
            val detail: HandleUntaggedExceptionDetail = when (response.code)  {
                400 -> HandleUntaggedBadRequest(statusCode = response.code, headers = response.headers, rawBodyBytes = responseBytes)
                else ->  {
                    HandleUntaggedUnexpected(statusCode = response.code, headers = response.headers, rawBodyBytes = responseBytes)
                }
            }
            throw HandleUntaggedException(detail = detail, statusCode = response.code, status = response.message, body = responseText, headers = response.headers, rawBodyBytes = responseBytes)
        }

        val responseBytes = response.body?.bytes() ?: ByteArray(0)
        val responseText = responseBytes.toString(Charsets.UTF_8)
        var status200: UntaggedEnum? = null

        if (response.code == 200) {
            status200 = gson.fromJson<UntaggedEnum>(responseText.ifEmpty { "null" }, object : TypeToken<UntaggedEnum>() {}.type)
        }
        return HandleUntaggedResponse(statusCode = response.code, raw = response, status200 = status200)
    }
}
