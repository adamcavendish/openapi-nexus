# Code generated by openapi-nexus. DO NOT EDIT.
#
# Discriminated Union with Long Type Names Test — 1.0.0
# Test fixture for discriminated union where variant type names are
# intentionally long enough to force multi-line type alias output at the
# 100-char render width. Ensures PEP 695 parenthesization is correct.

from __future__ import annotations

from collections.abc import Mapping

from ..models.very_long_discriminated_union_type_name_for_testing20260630 import VeryLongDiscriminatedUnionTypeNameForTesting20260630
from ..runtime.client import ApiResponse, Client
from ..runtime.errors import ApiError

class CreateResourceUnexpected:
    def __init__(self, status_code: int, headers: Mapping[str, str], raw_body: bytes) -> None:
        self.status_code: int = status_code
        self.headers: Mapping[str, str] = headers
        self.raw_body: bytes = raw_body
    @property
    def body(self) -> bytes:
        return self.raw_body

type CreateResourceErrorDetail = (
    CreateResourceUnexpected
)

class CreateResourceError(ApiError):
    def __init__(self, status_code: int, status: str, body: bytes, detail: CreateResourceErrorDetail, headers: Mapping[str, str] | None = None, response: object | None = None) -> None:
        self.detail: CreateResourceErrorDetail = detail
        super().__init__(status_code, status, body, headers=headers, response=response)


class DefaultApi:
    def __init__(self, client: Client) -> None:
        self._client = client

    def create_resource_with_http_info(self, *, body: VeryLongDiscriminatedUnionTypeNameForTesting20260630) -> ApiResponse[VeryLongDiscriminatedUnionTypeNameForTesting20260630]:
        """Create resource, including HTTP response metadata."""
        path = "/resource"
        headers: dict[str, str] = {}
        headers["Content-Type"] = "application/json"
        response = self._client.request("POST", path, json=body, headers=headers)
        if not (200 <= response.status_code < 300):
            detail = CreateResourceUnexpected(response.status_code, response.headers, response.content)
            raise CreateResourceError(response.status_code, response.reason, response.content, detail, headers=response.headers, response=response)
        data = response.json()  # type: ignore[return-value]
        return ApiResponse(data=data, status_code=response.status_code, headers=response.headers, raw=response)

    def create_resource(self, *, body: VeryLongDiscriminatedUnionTypeNameForTesting20260630) -> VeryLongDiscriminatedUnionTypeNameForTesting20260630:
        """Create resource."""
        return self.create_resource_with_http_info(body=body).data
