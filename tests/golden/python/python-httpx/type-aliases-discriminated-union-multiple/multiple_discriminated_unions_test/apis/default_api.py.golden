# Code generated by openapi-nexus. DO NOT EDIT.
#
# Multiple Discriminated Unions Test — 1.0.0
# Test fixture with multiple discriminated unions to verify Kind types
# are properly namespaced and don't conflict.
# This reproduces the original bug where both unions would generate
# the same Kind.ts file causing conflicts.

from __future__ import annotations

from collections.abc import Mapping

from ..models.container_kind import ContainerKind
from ..models.volume_kind import VolumeKind
from ..runtime.client import Client
from ..runtime.errors import ApiError

class CreateContainerUnexpected:
    def __init__(self, status_code: int, headers: Mapping[str, str], raw_body: bytes) -> None:
        self.status_code: int = status_code
        self.headers: Mapping[str, str] = headers
        self.raw_body: bytes = raw_body
    @property
    def body(self) -> bytes:
        return self.raw_body

type CreateContainerErrorDetail = (
    CreateContainerUnexpected
)

class CreateContainerError(ApiError):
    def __init__(self, status_code: int, status: str, body: bytes, detail: CreateContainerErrorDetail, headers: Mapping[str, str] | None = None, response: object | None = None) -> None:
        self.detail: CreateContainerErrorDetail = detail
        super().__init__(status_code, status, body, headers=headers, response=response)

class CreateVolumeUnexpected:
    def __init__(self, status_code: int, headers: Mapping[str, str], raw_body: bytes) -> None:
        self.status_code: int = status_code
        self.headers: Mapping[str, str] = headers
        self.raw_body: bytes = raw_body
    @property
    def body(self) -> bytes:
        return self.raw_body

type CreateVolumeErrorDetail = (
    CreateVolumeUnexpected
)

class CreateVolumeError(ApiError):
    def __init__(self, status_code: int, status: str, body: bytes, detail: CreateVolumeErrorDetail, headers: Mapping[str, str] | None = None, response: object | None = None) -> None:
        self.detail: CreateVolumeErrorDetail = detail
        super().__init__(status_code, status, body, headers=headers, response=response)


class DefaultApi:
    def __init__(self, client: Client) -> None:
        self._client = client

    def create_container(self, *, body: ContainerKind) -> ContainerKind:
        """Create container with ContainerKind discriminated union."""
        path = "/container"
        headers: dict[str, str] = {}
        headers["Content-Type"] = "application/json"
        response = self._client.request("POST", path, json=body, headers=headers)
        if not (200 <= response.status_code < 300):
            detail = CreateContainerUnexpected(response.status_code, response.headers, response.content)
            raise CreateContainerError(response.status_code, response.reason_phrase, response.content, detail, headers=response.headers, response=response)
        return response.json()  # type: ignore[return-value]

    def create_volume(self, *, body: VolumeKind) -> VolumeKind:
        """Create volume with VolumeKind discriminated union."""
        path = "/volume"
        headers: dict[str, str] = {}
        headers["Content-Type"] = "application/json"
        response = self._client.request("POST", path, json=body, headers=headers)
        if not (200 <= response.status_code < 300):
            detail = CreateVolumeUnexpected(response.status_code, response.headers, response.content)
            raise CreateVolumeError(response.status_code, response.reason_phrase, response.content, detail, headers=response.headers, response=response)
        return response.json()  # type: ignore[return-value]
