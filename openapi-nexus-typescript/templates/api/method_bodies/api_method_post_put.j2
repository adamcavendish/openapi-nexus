  // Build path with path parameters
  let urlPath = `{{ path | replace('{', '${') }}`;

  // Build query parameters
  const queryParameters: any = {};
{% for param in query_params %}
  if ({{ param.name }} !== undefined) {
    queryParameters['{{ param.name }}'] = {{ param.name }};
  }
{% endfor %}

  // Build headers
  const headerParameters: Record<string, string> = {
    {% if body_param %}'Content-Type': 'application/json',{% endif %}
    ...this.configuration?.headers,
  };

  // Add header parameters
{% for param in header_params %}
  if ({{ param.name }} !== undefined) {
    headerParameters['{{ param.name }}'] = String({{ param.name }});
  }
{% endfor %}

  // Prepare request body
  const body = {% if body_param %}{{ body_param.name }}{% else %}undefined{% endif %};

  // Make request
  const response = await this.request({
      path: urlPath,
      method: '{{ http_method }}',
      headers: headerParameters,
      query: queryParameters,
      body,
  }, initOverrides);

  return new JSONApiResponse(response);
